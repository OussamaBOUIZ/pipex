/*
	Let's suppose that we receive the command line as a linked list, 
	A command may take input either from a file or as output of another
	command through a pipe.
	Similarl
	y a command may outputs eithor on a file or on another
	command through a pipe.

	For each command we'll have to create a child proc


	? The Question's what happens exactly after fork() cal
		
*/


